<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="model"
            type="com.mockcommerce.models.ShipmentAddressModel" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="8dp"
            android:buttonTint="@color/colorPrimary"
            android:checked="@{model.selected}"
            android:clickable="false"
            android:focusable="false"
            android:gravity="center"
            tools:checked="true" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lines="1"
                android:maxLines="1"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:text="@{model.title}"
                android:textColor="@color/black"
                android:textSize="20sp"
                tools:text="Ev" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:lines="1"
                android:maxLines="2"
                android:text="@{model.address}"
                android:textSize="16sp"
                tools:text="Fırtına Mahellesi, Uçurum Sitesi, Blok 42, Daire 3, Kadırga" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lines="1"
                android:maxLines="1"
                android:text="@{model.district + '/' + model.city}"
                android:textSize="16sp"
                tools:text="Sarıyer/İstanbul" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lines="1"
                android:maxLines="1"
                android:text="@{model.receiver + '-' + model.phoneNumber}"
                android:textSize="16sp"
                tools:text="Ali Can - 123 4567890" />

        </LinearLayout>
    </LinearLayout>
</layout>